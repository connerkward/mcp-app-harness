{
  "name": "mcp-app-harness",
  "version": "0.1.0",
  "type": "module",
  "description": "Quick-launch testing harness for MCP Apps UI — wraps the official ext-apps basic-host",
  "scripts": {
    "setup": "cd ext-apps/examples/basic-host && npm install",
    "start": "node run.mjs",
    "test": "node run.mjs"
  }
}
